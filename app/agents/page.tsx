import { AgentTokenManager } from "./agent-token-manager";

export const metadata = {
  title: "Agent Tokens · Laniameda",
};

export default function AgentsPage() {
  return <AgentTokenManager />;
}
